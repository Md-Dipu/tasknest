# Environment Configuration
# Copy this file to .env and update the values with your actual configuration

# Application Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# MongoDB Database Configuration
# Replace with your MongoDB connection string
# For local development: mongodb://localhost:27017/tasknest
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database-name
MONGO_URI=mongodb://localhost:27017/tasknest

# Session Configuration
# Generate a secure random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secure-session-secret-key-here

# Optional: Additional Configuration
# Uncomment and set these if needed in your application

# Email Configuration (if implementing email features)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# File Upload Configuration (if implementing file uploads)
# MAX_FILE_SIZE=5242880
# UPLOAD_PATH=./uploads

# Redis Configuration (if implementing caching)
# REDIS_URL=redis://localhost:6379

# JWT Configuration (if implementing JWT tokens)
# JWT_SECRET=your-jwt-secret-key
# JWT_EXPIRES_IN=7d
