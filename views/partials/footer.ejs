<footer class="bg-gray-800 text-gray-200 py-12">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      <!-- Branding -->
      <div>
        <h3 class="text-xl font-bold mb-4">tasknest</h3>
        <p class="text-gray-400 mb-4">
          Your personalized task manager for students, professionals, and
          religious users.
        </p>
        <p class="text-gray-400">
          Empowering productivity with intuitive Kanban boards tailored to your
          unique needs.
        </p>
      </div>

      <!-- Navigation -->
      <div>
        <h3 class="text-lg font-semibold mb-4">Navigate</h3>
        <ul class="space-y-2">
          <li>
            <a
              href="/"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="Home"
              >Home</a
            >
          </li>
          <li>
            <a
              href="/auth/register"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="Get Started"
              >Get Started</a
            >
          </li>
          <li>
            <a
              href="/auth/login"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="Log In"
              >Log In</a
            >
          </li>
          <li>
            <a
              href="#about"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="About"
              >About</a
            >
          </li>
          <li>
            <a
              href="#contact"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="Contact"
              >Contact</a
            >
          </li>
        </ul>
      </div>

      <!-- Resources -->
      <div>
        <h3 class="text-lg font-semibold mb-4">Resources</h3>
        <ul class="space-y-2">
          <li>
            <a
              href="#faq"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="FAQ"
              >FAQ</a
            >
          </li>
          <li>
            <a
              href="#support"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="Support"
              >Support</a
            >
          </li>
          <li>
            <a
              href="#blog"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="Blog"
              >Blog</a
            >
          </li>
        </ul>
      </div>

      <!-- Contact -->
      <div>
        <h3 class="text-lg font-semibold mb-4">Contact Us</h3>
        <ul class="space-y-2">
          <li>
            <a
              href="mailto:support@tasknest.com"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="Email Support"
              >support@tasknest.com</a
            >
          </li>
          <li>
            <a
              href="#support"
              class="hover:<%= user && user.userType === 'student' ? 'text-indigo-500' : user && user.userType === 'professional' ? 'text-teal-500' : 'text-purple-500' %> transition-colors"
              aria-label="Support Center"
              >Support Center</a
            >
          </li>
        </ul>
      </div>
    </div>

    <!-- Copyright -->
    <div class="mt-8 text-center text-gray-400">
      <p>
        ©
        <script>
          document.write(new Date().getFullYear());
        </script>
        tasknest. All rights reserved.
      </p>
    </div>
  </div>
</footer>
